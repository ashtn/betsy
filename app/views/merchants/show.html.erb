<h1> <%= @merchant.username %> Order Fulfillment</h1>

<p>Find me in app/views/merchants/show.html.erb</p>

<section>
  <h4>Total Revenue:</h3>
  $<%= sprintf('%.2f', @merchant.revenue) %>
</section>

<%= render partial: "totals_by_status" %>

<%= render partial: "merchant_orders" %>

<%= render partial: "order_items" %>

<button><%= link_to "Add New Item", new_item_path %></button>
<button><%= link_to "Add New Category", new_category_path %></button>
