<section>

  <h4> Merchant_orders :</h4>

  <table>
    <thead>
      <th>
        Order Id
      </th>
      <th>
        order total
      </th>
    </thead>
    <% @merchant.merchant_orders.each do |order| %>
    <tr>
      <%# raise %>
      <td>
        <%=link_to "#{order.id}", order_path(order.id), method: :get%>
      </td>
      <td>
        $<%= sprintf('%.2f', order.total)%>
      </td>
      <%end%>
    </tr>
  </table>
</section>
