
<table>
  <thead>
    <th>
      Item
    </th>
    <th>
      Price
    </th>
    <th>
      Qty in Stock
    </th>
    <th>
      Average Rating
    </th>
    <th>

    </th>
    <th>

    </th>
  </thead>
<% @items.sort.each do |item| %>
  <tr>
    <td>
      <%=link_to "#{item.name.capitalize}", item_path(item.id), method: :get%>
    </td>
    <td>
      <%= "#{item.price}"%>
    </td>
    <td>
      <%= "#{item.inventory}"%>
    </td>
    <td>
      <%= "#{item.average_rating}" %>
    </td>
    <% if !session[:merchant_id] %>
    <td>
        <%= link_to "Cart", add_to_cart_path(item.id),  method: :patch%>
    </td>
    <%end%>
    <% if session[:merchant_id] %>
    <td>
      <%= link_to "⚙️", edit_item_path(item.id), method: :get %>
    </td>
    <td>
      <%= link_to "🗑", item_path(item.id), method: :delete %>
    </td>
    <td>
      <%= link_to "🔒", retire_path(item.id), method: :patch %>
    </td>
    <%end%>
  </tr>
  <%end%>

</table>




<% if session[:merchant_id] %>
<%= link_to "Add New Item", new_item_path, method: :get %>
<% end %>
