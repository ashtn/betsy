
<table>
  <thead>
    <th>
      Item
    </th>
    <th>
      Price
    </th>
    <th>
      Qty in Stock
    </th>
    <th>

    </th>
    <th>

    </th>
  </thead>
<% @items.each do |item| %>
  <tr>
    <td>
      <%=link_to "#{item.name.capitalize}", item_path(item.id), method: :get%>
    </td>
    <td>
      <%= "#{item.price}"%>
    </td>
    <td>
      <%= "#{item.inventory}"%>
    </td>
    <% if !session[:id] %>
    <td>
        <%= link_to "Cart", add_to_cart_path(item.id),  method: :patch%>
    </td>
    <%end%>
    <% if session[:id] %>
    <td>
      <%= link_to "Update", edit_item_path(item.id), method: :get %>
    </td>
    <td>
      <%= link_to "Delete", item_path(item.id), method: :delete %>
    </td>
    <td>
      <%= link_to "Retire", item_path(item.id), method: :delete %>
    </td>
    <%end%>
  </tr>
  <%end%>

</table>





<%= link_to "Add New Item", new_item_path, method: :get %>
